---
import { getTranslations } from '../../shared/utils/i18n';

interface Props {
  lang?: string;
}

const { lang = 'es' } = Astro.props;
const t = getTranslations(lang);
---

<section id="about" class="py-20 bg-white">
  <div class="container mx-auto px-4">
    <div class="max-w-6xl mx-auto">
      <!-- Main About Section -->
      <div class="text-center space-y-6 mb-16">
        <h2 class="text-4xl font-bold text-green-800">{t.about.title}</h2>
        <p class="text-lg text-gray-600 leading-relaxed max-w-3xl mx-auto">
          {t.about.description}
        </p>
      </div>
      
      <!-- Quality Features -->
      <div class="grid md:grid-cols-3 gap-8 mb-16">
        <div class="p-6 bg-green-50 rounded-lg text-center">
          <div class="text-4xl mb-4">🌿</div>
          <h3 class="text-xl font-semibold mb-2 text-green-700">100% Natural</h3>
          <p class="text-gray-600">
            {lang === 'es' ? 'Sin químicos ni aditivos' : lang === 'it' ? 'Senza sostanze chimiche o additivi' : 'No chemicals or additives'}
          </p>
        </div>
        
        <div class="p-6 bg-green-50 rounded-lg text-center">
          <div class="text-4xl mb-4">✨</div>
          <h3 class="text-xl font-semibold mb-2 text-green-700">
            {lang === 'es' ? 'Premium' : lang === 'it' ? 'Qualità Premium' : 'Premium Quality'}
          </h3>
          <p class="text-gray-600">
            {lang === 'es' ? 'Selección cuidadosa' : lang === 'it' ? 'Selezione accurata' : 'Carefully selected'}
          </p>
        </div>
        
        <div class="p-6 bg-green-50 rounded-lg text-center">
          <div class="text-4xl mb-4">🏆</div>
          <h3 class="text-xl font-semibold mb-2 text-green-700">
            {lang === 'es' ? 'Certificado' : lang === 'it' ? 'Certificato' : 'Certified'}
          </h3>
          <p class="text-gray-600">
            {lang === 'es' ? 'Calidad garantizada' : lang === 'it' ? 'Qualità garantita' : 'Quality guaranteed'}
          </p>
        </div>
      </div>

      <!-- Export Strategy Section -->
      <div class="bg-gradient-to-r from-green-50 to-emerald-50 rounded-2xl p-8 mb-12">
        <h3 class="text-3xl font-bold text-green-800 mb-6 text-center">{t.about.exportTitle}</h3>
        <p class="text-gray-700 text-center mb-8 max-w-3xl mx-auto">{t.about.exportDescription}</p>
        
        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
          <div class="bg-white rounded-lg p-6 shadow-sm">
            <div class="text-3xl mb-3">📈</div>
            <h4 class="font-semibold text-green-700 mb-2">{t.about.marketGrowth}</h4>
            <p class="text-sm text-gray-600">{t.about.marketGrowthText}</p>
          </div>
          
          <div class="bg-white rounded-lg p-6 shadow-sm">
            <div class="text-3xl mb-3">💎</div>
            <h4 class="font-semibold text-green-700 mb-2">{t.about.valueProposition}</h4>
            <p class="text-sm text-gray-600">{t.about.valuePropositionText}</p>
          </div>
          
          <div class="bg-white rounded-lg p-6 shadow-sm">
            <div class="text-3xl mb-3">📋</div>
            <h4 class="font-semibold text-green-700 mb-2">{t.about.certifications}</h4>
            <p class="text-sm text-gray-600">{t.about.certificationsText}</p>
          </div>
          
          <div class="bg-white rounded-lg p-6 shadow-sm">
            <div class="text-3xl mb-3">🎯</div>
            <h4 class="font-semibold text-green-700 mb-2">{t.about.competitiveAdvantage}</h4>
            <p class="text-sm text-gray-600">{t.about.competitiveAdvantageText}</p>
          </div>
        </div>
      </div>

      <!-- Technical Highlights -->
      <div class="grid md:grid-cols-2 gap-8">
        <div class="bg-blue-50 rounded-lg p-6">
          <h4 class="text-xl font-semibold text-blue-800 mb-4">
            {lang === 'es' ? 'Tecnología Aplicada' : lang === 'it' ? 'Tecnologia Applicata' : 'Applied Technology'}
          </h4>
          <ul class="space-y-2 text-gray-700">
            <li class="flex items-start">
              <span class="text-blue-500 mr-2 mt-1">•</span>
              <span>{lang === 'es' ? 'Selección varietal optimizada' : lang === 'it' ? 'Selezione varietale ottimizzata' : 'Optimized varietal selection'}</span>
            </li>
            <li class="flex items-start">
              <span class="text-blue-500 mr-2 mt-1">•</span>
              <span>{lang === 'es' ? 'Sistemas de riego controlados' : lang === 'it' ? 'Sistemi di irrigazione controllati' : 'Controlled irrigation systems'}</span>
            </li>
            <li class="flex items-start">
              <span class="text-blue-500 mr-2 mt-1">•</span>
              <span>{lang === 'es' ? 'Secado controlado para preservar aceites' : lang === 'it' ? 'Essiccazione controllata per preservare gli oli' : 'Controlled drying to preserve oils'}</span>
            </li>
          </ul>
        </div>
        
        <div class="bg-orange-50 rounded-lg p-6">
          <h4 class="text-xl font-semibold text-orange-800 mb-4">
            {lang === 'es' ? 'Sostenibilidad Ambiental' : lang === 'it' ? 'Sostenibilità Ambientale' : 'Environmental Sustainability'}
          </h4>
          <ul class="space-y-2 text-gray-700">
            <li class="flex items-start">
              <span class="text-orange-500 mr-2 mt-1">•</span>
              <span>{lang === 'es' ? 'Control biológico de plagas' : lang === 'it' ? 'Controllo biologico dei parassiti' : 'Biological pest control'}</span>
            </li>
            <li class="flex items-start">
              <span class="text-orange-500 mr-2 mt-1">•</span>
              <span>{lang === 'es' ? 'Prácticas de cultivo ecológicas' : lang === 'it' ? 'Pratiche di coltivazione ecologiche' : 'Ecological cultivation practices'}</span>
            </li>
            <li class="flex items-start">
              <span class="text-orange-500 mr-2 mt-1">•</span>
              <span>{lang === 'es' ? 'Cuidado ambiental integral' : lang === 'it' ? 'Cura ambientale integrale' : 'Comprehensive environmental care'}</span>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>
